import {sqliteTable,text,integer,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const content=sqliteTable('content',{id:text('id').primaryKey(),kind:text('kind').notNull(),slug:text('slug').notNull(),title:text('title').notNull(),data:text('data').notNull(),published:integer('published').notNull().default(0),sortOrder:integer('sort_order').notNull().default(0),updated:text('updated').notNull()},t=>[uniqueIndex('content_kind_slug').on(t.kind,t.slug)]);
export const enquiries=sqliteTable('enquiries',{id:text('id').primaryKey(),token:text('token').notNull().unique(),created:text('created').notNull(),name:text('name').notNull(),email:text('email').notNull(),data:text('data').notNull(),files:text('files').notNull().default('[]'),status:text('status').notNull().default('New'),notification:text('notification').notNull().default('Not configured'),ready:integer('ready').notNull().default(0)});
export const media=sqliteTable('media',{id:text('id').primaryKey(),key:text('key').notNull(),mime:text('mime').notNull(),filename:text('filename').notNull(),created:text('created').notNull()});
export const limits=sqliteTable('rate_limits',{key:text('key').primaryKey(),count:integer('count').notNull().default(1),expires:integer('expires').notNull()});
